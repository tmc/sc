# ML Experiments

The historical ML experiment portfolio was extracted to the adjacent
repository `../sc-ml`. Treat `../sc-ml` as the source of truth for
mirrored experiments, result artifacts, and supporting MLX
infrastructure.

This repository now retains a single local experiment package:

- `ml/experiments/exp_neurosymbolic_statecharts`
  Integrated neuro-symbolic architecture work that has not been folded
  into `../sc-ml`.

The duplicate retained directories were removed from this repository
after verification against `../sc-ml`.
